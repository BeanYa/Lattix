根级 `go work use` 已声明 agent/backend/shared 三个子模块；Docker 构建命令 `docker build -t lattix-backend .` 会同时完成前端 dist 生成与后端二进制编译；一键安装通过 `curl -fsSL https://raw.githubusercontent.com/BeanYa/Lattix/main/install.sh | bash` 调用交互式向导或 `install.sh panel --mode docker|native` / `install.sh agent ...` 直接部署。