- 所有设计文档使用中文撰写，但保留 Go 标识符、文件名、包名、CLI 命令与协议字段名（如 `node.apply`、`agent.session.open`、`X-Request-ID`）为原始英文形式。
- 跨文档引用采用章节编号（如 §6、§13、§17、§21）与文件名双标注方式，确保设计变更时可追溯。
- 协议类文档严格区分“MVP 范围”与“后续迭代”，对不在 MVP 的内容明确标注“自 0.0.2 之后迭代实现”，避免误实现。
- 机器可读契约（OpenAPI / JSON Schema）与人类可读设计文档并列维护，前者作为权威来源，后者解释语义与约束背景。