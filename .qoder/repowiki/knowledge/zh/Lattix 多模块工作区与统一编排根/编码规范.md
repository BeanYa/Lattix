- 所有 Go 子模块通过 go.work 统一管理，禁止在根目录直接 go mod init
- Docker 构建使用三阶段分离（bun 前端 → golang 后端 → alpine 运行），最终镜像仅包含单一二进制
- 安装脚本遵循 install.sh → install-<component>.sh 的分发模式，版本号由 GitHub Release tag 驱动
- 环境变量以 LATTIX_ 前缀命名（如 LATTIX_DEPLOY_MODE、LATTIX_DB、LATTIX_TLS_DIR）用于配置注入